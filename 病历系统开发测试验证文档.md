# 诊所病历系统开发、测试与验证文档

## 1. 文档目的

本文档是诊所单机病历系统的统一开发、测试、验收和发布依据。

后续实现应以本文档中的数据结构、交互约束、错误处理、性能指标和自动化测试场景为准。需求发生变化时，应先更新本文档，再修改代码和测试。

## 2. 项目范围

### 2.1 目标

构建一个 Windows PC 端单机病历系统，满足以下要求：

- 病历列表、新增、编辑、查看、查询和单条删除
- 删除操作必须二次确认
- 操作步骤可记录和导出，底层保留历史快照，界面不提供撤销或重做入口
- 支持导入、导出与现有 Excel 结构兼容的病历数据
- Excel 导入逐行校验，支持重复数据跳过、保留、覆盖或逐条处理
- 支持低配置电脑流畅运行
- 支持常见 Windows 分辨率
- 支持键盘快捷键
- 可打包为直接运行的 Windows 便携版 exe，而非安装包
- 支持使用 Playwright 自动化测试和截图回归

### 2.2 非目标

第一版不包含以下内容：

- 云端同步
- 多机构联网
- 在线问诊
- 医保结算
- 复杂的多角色权限体系
- 移动端 App

如后续需要扩展，应通过新的需求和数据迁移方案加入，不直接破坏现有本地数据库结构。

## 3. 已有 Excel 数据分析

输入文件：`历史病历.xlsx`

- 工作表：`病例汇总`
- 数据行：8075 条
- 字段数：9 个
- 完整 9 字段完全重复：0 组
- `日期 + 姓名` 重复：134 组，共 204 条重复记录
- `日期 + 姓名 + 性别 + 年龄` 重复：125 组，共 188 条重复记录

现有字段如下：

| 顺序 | 字段 | 建议存储类型 | 导入要求 |
|---:|---|---|---|
| 1 | 日期 | ISO 日期字符串 | 必填，可解析 Excel 日期、日期序列号和常见文本日期 |
| 2 | 姓名 | 文本 | 必填，去除首尾空白，不自动修改姓名内容 |
| 3 | 性别 | 文本/枚举 | 可为空，常见值为“男”“女”，异常值产生警告 |
| 4 | 年龄 | 原始文本 | 不强制整数，兼容“17”“1岁”“2岁10个月”等表达 |
| 5 | 诊断 | 长文本 | 可为空 |
| 6 | 临床表现 | 长文本 | 可为空 |
| 7 | 治疗 | 长文本 | 可为空 |
| 8 | 备注 | 长文本 | 可为空 |
| 9 | 费用 | 原始文本 | 兼容“295”“100-80”等金额或组合表达，不丢失原文 |

### 3.1 数据建模原则

- 年龄和费用按原始文本保存，避免破坏历史数据。
- 对可规范化的值提供 `normalized_value` 或校验提示，但不覆盖 `raw_value`。
- 每条病历使用系统内部唯一 ID，不以姓名作为主键。
- 日期统一以 `YYYY-MM-DD` 保存，导出时再按用户选择格式化。
- 空单元格保存为 `NULL`，不保存为字符串 `"null"` 或 `"undefined"`。

## 4. 技术方案

### 4.1 已落地技术栈

- 桌面壳：Electron
- 构建：electron-vite
- 前端：Vue 3、TypeScript、Vite
- UI：Element Plus
- 状态管理：Vue Composition API，本版为单工作台界面，不引入跨页面状态复杂度
- 数据库：Electron/Node 内置 `node:sqlite`，不依赖原生扩展重新编译
- Excel：`exceljs`
- 桌面打包：electron-builder `portable` 目标
- 自动化测试：Playwright Test
- 代码质量：ESLint、TypeScript strict mode

### 4.2 选择 Electron 的原因

- Playwright 有成熟的 Electron 启动与控制能力。
- 可以生成 Windows portable 单文件 exe。
- Vue 渲染层可以在浏览器中单独测试，桌面层可以做启动和 IPC 测试。
- 文件系统、SQLite 和导入导出任务可以放在主进程，减少渲染进程阻塞。

Electron 的资源占用高于 Tauri。本版通过数据库分页、最多 100 条的页面渲染、输入防抖、SQLite 普通索引和 FTS5 trigram 全文索引控制资源消耗，不把完整数据集载入渲染进程。若实际部署环境内存极低，可以保留本文档定义的 API 接口替换桌面壳，但替换前需重新确认 Playwright 桌面自动化方案。

### 4.3 进程边界

```text
Electron 页面 -> preload 暴露的类型化 API -> Electron IPC --┐
                                                            ├-> Repository / Excel / Backup 服务 -> SQLite
浏览器页面 -> 127.0.0.1 上的开发期 HTTP API ---------------┘
```

约束：

- 渲染进程不直接访问 Node.js、文件系统或数据库。
- 所有 IPC 参数使用共享 TypeScript 类型定义。
- preload 使用 `contextIsolation: true` 和 `nodeIntegration: false`。
- 数据库迁移必须有版本号，禁止通过手动改表结构发布。
- 导入和导出不得在渲染进程执行大循环。

### 4.4 推荐目录结构

```text
src/
  renderer/
    pages/
    components/
    stores/
    composables/
    router/
  main/
    db/
      migrations/
      repositories/
    services/
      import/
      export/
      operation-log/
      backup/
  preload/
  shared/
    types/
    validation/
    constants/
tests/
  e2e/
  fixtures/
  helpers/
```

## 5. 数据库设计

### 5.1 `medical_records`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER | 主键，自增 |
| `visit_date` | TEXT | `YYYY-MM-DD`，必填 |
| `name` | TEXT | 姓名，必填 |
| `gender` | TEXT | 性别，可为空 |
| `age_raw` | TEXT | 年龄原始文本 |
| `diagnosis` | TEXT | 诊断 |
| `clinical_manifestation` | TEXT | 临床表现 |
| `treatment` | TEXT | 治疗 |
| `remark` | TEXT | 备注 |
| `fee_raw` | TEXT | 费用原始文本 |
| `created_at` | TEXT | 创建时间 |
| `updated_at` | TEXT | 更新时间 |
| `source` | TEXT | `manual`、`import`、`restore` |

索引：

- `visit_date`
- `name`
- `gender`
- `diagnosis`
- `(visit_date, name)` 组合索引

### 5.2 `operation_logs`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER | 主键 |
| `operation_type` | TEXT | 新增、编辑、删除、导入、导出、恢复等 |
| `source` | TEXT | 手工、Excel、系统恢复 |
| `affected_record_ids` | TEXT | JSON 数组 |
| `before_data` | TEXT | 操作前 JSON 快照 |
| `after_data` | TEXT | 操作后 JSON 快照 |
| `created_at` | TEXT | 操作时间 |
| `undone_at` | TEXT | 回退时间，可为空 |
| `metadata` | TEXT | 文件名、行号、重复策略等信息 |

### 5.3 `app_settings`

保存以下配置：

- 表格密度
- 字体缩放比例
- 默认分页大小
- 默认重复判断规则
- 数据目录
- 自动备份开关和保留份数
- 最近打开文件
- 窗口尺寸和位置

## 6. 页面与交互设计

### 6.1 病历列表页

必须提供：

- 搜索框
- 日期范围
- 性别筛选
- 诊断/治疗文本筛选
- 重置筛选
- 新增按钮
- 导入按钮
- 导出按钮
- 单条删除按钮
- 操作记录入口
- 表格列设置
- 新建表单提供“保存并继续新建”，成功保存后保持抽屉打开、清空表单并聚焦姓名输入框

表格要求：

- 默认按日期倒序、ID 倒序排列
- 默认每页 50 条，可切换 20、50、100 条
- 支持横向滚动
- 普通列支持拖动表头分隔线调整列宽
- 表格存在横向或纵向滚动范围时，滚动条始终显示，不依赖鼠标移入
- 日期、姓名、性别、年龄和操作列使用紧凑默认宽度
- 空状态、加载状态、错误状态必须分别设计
- 列表不显示独立页面标题区，顶栏下直接显示筛选区，将垂直空间留给表格
- 表格工具栏左侧显示“共 x 张病历”，右侧显示“新增病历”；不显示复选框或批量删除入口
- 单击非复选框、非操作列区域时打开病历明细
- 主页面固定在视口内，数据加载或导入后不得出现多余的页面级纵向滚动条
- 顶栏固定为 60px 高；页面标题、筛选区、表格工具栏和分页区采用紧凑纵向间距
- 页面与表格面板采用紧凑水平留白；1024px 宽度下内容区左右留白为 10px
- 开始日期、结束日期和性别之间保持 10px 间距，查询和重置按钮之间保持 4px 间距
- 日期列固定按后端默认顺序展示，不提供表头排序入口
- 在最低支持宽度 1024px 下，关键词、起止日期、性别和查询按钮必须保持同一行

### 6.2 新增和编辑

- 新增和编辑共用同一个 `min(640px, 94vw)` 右侧抽屉及完全一致的双列表单布局。
- 日期、姓名必填。
- 保存前进行前端校验，保存时由主进程再次校验。
- 1024 x 768 下，日期、姓名、性别、年龄、诊断、费用、临床表现、治疗和备注应在一屏内完整展示，抽屉正文不产生滚动条。
- 点击抽屉外空白不关闭新建或编辑抽屉。
- 新建已填写内容或编辑发生修改后，点击取消或右上角关闭按钮必须显示“保存并关闭 / 不保存 / 继续编辑”三项选择。
- 选择保存但校验失败时保留表单并停留在编辑状态。
- 保存成功后返回列表并定位到新记录或被修改记录。
- 保存失败时保留用户输入，不清空表单。
- 所有日期选择器使用简体中文月份、星期和操作文案。

### 6.3 病历明细

- 使用宽度为 `min(720px, 92vw)` 的右侧抽屉，兼顾长文本和窄窗口。
- 点击页面遮罩不关闭明细，避免查看长文本时误触丢失阅读位置。
- 仅通过关闭按钮、右上角关闭图标或进入编辑操作关闭明细。

### 6.4 删除确认

删除弹窗至少显示：

- 待删除病历的明确提示
- 删除前会自动创建数据库备份
- “取消”和“确认删除”两个明确按钮

确认删除后：

- 使用 SQLite 事务
- 删除成功后刷新当前页
- 如果当前页为空，自动回到上一页
- 写入一条包含操作前快照的记录

### 6.5 操作记录页

支持：

- 按时间、操作类型、来源筛选
- 查看前后数据差异
- 导出操作日志
- 显示导入批次的文件名和统计信息

## 7. Excel 导入规范

### 7.1 导入流程

```text
选择文件
  -> 读取工作表
  -> 表头映射
  -> 逐行校验
  -> 重复检测
  -> 预览统计
  -> 用户确认策略
  -> 事务导入
  -> 生成报告
```

### 7.2 表头映射

默认映射：

```text
日期 -> visit_date
姓名 -> name
性别 -> gender
年龄 -> age_raw
诊断 -> diagnosis
临床表现 -> clinical_manifestation
治疗 -> treatment
备注 -> remark
费用 -> fee_raw
```

如果表头缺少必填字段，导入预览直接阻止提交，并指出缺失列。

### 7.3 逐行校验

每行产生以下状态之一：

- `valid`：可以直接导入
- `warning`：可以导入，但存在格式风险
- `error`：必须修正或跳过
- `duplicate`：与现有数据或本次导入其他行重复

校验规则：

| 字段 | 错误条件 | 警告条件 |
|---|---|---|
| 日期 | 为空或无法解析 | 使用了不常见格式 |
| 姓名 | 为空 | 超过建议长度 |
| 性别 | 无 | 非男/女/未知 |
| 年龄 | 无 | 无法识别为常见年龄表达 |
| 诊断 | 无 | 超过建议长度 |
| 临床表现 | 无 | 文本过长 |
| 治疗 | 无 | 文本过长 |
| 备注 | 无 | 文本过长 |
| 费用 | 无 | 非纯数字或包含多个金额段 |

异常数据必须保留原始内容，并在报告中记录 Excel 行号、字段名、原始值和提示信息。

### 7.4 重复策略

支持以下匹配规则：

1. 全部 9 个字段完全相同
2. 日期 + 姓名
3. 日期 + 姓名 + 性别 + 年龄
4. 用户自定义字段组合

支持以下处理方式：

- 跳过重复
- 保留为新记录
- 覆盖已有记录
- 对重复项逐条选择

默认策略建议为“日期 + 姓名 + 性别 + 年龄 + 诊断”，默认处理方式为“预览后跳过重复”。不得静默删除或覆盖数据。

### 7.5 导入事务和历史快照

- 导入前自动生成备份。
- 一个文件导入视为一个操作批次。
- 可选择“遇到错误行时停止”或“跳过错误行继续”。
- 提交导入使用事务，失败时整体回滚。
- 成功后写入一条导入操作日志。
- 操作日志保留整个导入批次的前后快照，当前界面不提供批次回退入口。

## 8. Excel 导出规范

支持三种范围：

- 当前筛选结果
- 当前选中记录
- 全部病历

默认导出 9 个业务字段，列顺序与原 Excel 一致。可选导出内部 ID、创建时间和更新时间。

导出要求：

- 在后台任务中执行，不阻塞界面。
- 显示导出进度和完成路径。
- 文件名默认包含导出类型和时间。
- 导出失败时显示明确错误原因。
- 支持导出校验失败行和操作日志。

## 9. 操作历史快照

底层采用命令记录方式保存历史快照：

- 新增：保存新增记录快照
- 编辑：保存 `before_data` 和 `after_data`
- 删除：保存删除前的完整记录
- 导入：保存整批新增或覆盖操作前后的记录
- 导出：记录导出行为，不改变病历数据

数据库层为旧版本数据兼容保留事务化撤销/重做实现，但渲染界面、菜单和键盘处理均不暴露该能力。

## 10. 键盘快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+N` | 新增病历 |
| `Ctrl+F` | 聚焦搜索框 |
| `Ctrl+S` | 保存当前编辑 |
| `Ctrl+Enter` | 提交表单 |
| `Esc` | 关闭抽屉或弹窗 |
| `Ctrl+I` | 打开 Excel 导入 |
| `Ctrl+E` | 导出当前结果 |
| `F5` | 刷新当前列表 |

输入框内的 `Ctrl+A`、文字编辑和浏览器默认行为不得被错误拦截。快捷键需要同时在菜单或按钮中提供可见入口。

## 11. 分辨率与视觉适配

目标分辨率：

- 最低：1024 x 768
- 常用：1366 x 768、1920 x 1080
- 高分辨率：4K 显示器及 Windows 缩放环境

要求：

- 顶部工具栏和页面纵向间距采用紧凑尺寸，优先增加列表可见行数。
- 筛选项使用响应式网格，并在 1024px 及以上保持单行。
- 表格允许横向滚动，不能压缩到无法阅读。
- 弹窗不超出屏幕可视区域。
- 支持紧凑和舒适两种表格密度。
- 支持字体缩放。
- 所有按钮、提示、表格内容不得互相遮挡。

Playwright 必须对 1024 x 768 和 1366 x 768 至少各执行一次截图回归。

## 12. 低配置性能方案

### 12.1 数据访问

- SQLite 开启 WAL。
- 列表查询必须带分页或明确限制。
- 日期、姓名、性别、诊断建立索引。
- 使用预编译 SQL。
- 批量导入使用事务。
- 不把全部病历一次性放入 Pinia。

### 12.2 前端渲染

- 使用虚拟滚动表格或分页表格。
- 搜索输入使用 200 至 300 毫秒防抖。
- 大文本详情按需加载。
- 导入和导出放入 Worker 或主进程任务。
- 生产环境关闭开发工具和 source map。
- 避免大范围响应式对象重复复制。
- 降低非必要动画和过渡效果。

### 12.3 性能验收目标

在 8 千至 10 万条记录数据集上验证：

- 首屏可交互时间不超过 2 秒。
- 普通姓名搜索 P95 不超过 300 毫秒。
- 日期和性别筛选 P95 不超过 300 毫秒。
- 8000 条 Excel 导入过程中界面持续可操作。
- 导出全部数据时有进度反馈。
- 应用重启后数据完整存在。

性能测试必须在至少一台低配置 Windows 电脑上执行，并记录 CPU、内存和耗时。

## 13. 本地数据、备份与隐私

默认数据库路径：

```text
%LOCALAPPDATA%\\ClinicRecords\\data\\clinic-records.db
```

便携 exe 同样使用上述用户数据目录，不把敏感数据写在 exe 旁边，避免只读目录、升级覆盖和多人拷贝 exe 时误带病历数据。测试可通过 `CLINIC_RECORDS_DATA_DIR` 指向隔离的临时目录。

必须实现：

- 手动备份
- 自动备份
- 备份文件时间戳
- 备份保留份数
- 导入前自动备份
- 删除前自动备份
- 数据库完整性检查
- 数据恢复前确认
- 恢复后自动重新加载列表

病历属于敏感数据，应用默认不联网、不上传。浏览器开发 API 仅监听 `127.0.0.1`，且不进入 portable 包。第一版不实现加密备份，导出文件和备份文件需按敏感数据管理。

## 14. IPC 接口约定

建议统一放在 `src/shared/types` 中定义请求和响应类型。

示例接口：

```ts
type RecordListQuery = {
  page: number;
  pageSize: number;
  keyword?: string;
  dateFrom?: string;
  dateTo?: string;
  gender?: string;
};

type RecordListResult = {
  rows: MedicalRecord[];
  total: number;
};

type ImportOptions = {
  duplicateKey: DuplicateKey;
  duplicateAction: DuplicateAction;
  invalidRowAction: 'stop' | 'skip';
};
```

建议 IPC 通道：

- `records:list`
- `records:get`
- `records:create`
- `records:update`
- `records:delete`
- `records:delete-batch`
- `records:undo`
- `records:redo`
- `operations:list`
- `operations:export`
- `import:analyze`
- `import:commit`
- `import:rollback`
- `export:records`
- `backup:create`
- `backup:restore`
- `settings:get`
- `settings:update`

每个接口都要有：

- 参数运行时校验
- 明确的错误码
- 可序列化的错误信息
- 超时和取消策略
- Playwright 可调用的测试入口

## 15. Playwright 测试架构

### 15.1 测试分层

1. 单元测试：校验器、重复判断、日期转换、费用原文处理。
2. 集成测试：SQLite Repository、事务、迁移、操作日志。
3. 页面测试：Vue 页面在浏览器环境中的交互。
4. Electron E2E：应用启动、IPC、文件导入导出和窗口行为。
5. 视觉回归：固定分辨率截图比较。

### 15.2 测试数据隔离

- 每个测试使用临时数据目录。
- 禁止直接修改正式用户数据。
- 每个测试开始前创建确定性数据库。
- 测试结束后清理临时目录。
- 日期、时间和随机 ID 可注入或固定。
- Excel 测试文件放入 `tests/fixtures`。

### 15.3 稳定定位标识

核心元素必须提供 `data-testid`：

```text
record-table
record-create-button
record-edit-button
record-row-delete
record-delete-confirm
record-search-input
record-filter-date
record-import-button
import-preview
import-row-status
import-duplicate-policy
import-submit
import-result
record-export-button
operation-log-table
backup-create-button
```

测试优先使用 `getByTestId`、可访问名称和角色定位，避免依赖 CSS 类名和表格行号。

### 15.4 必须覆盖的 E2E 场景

#### 病历 CRUD

- 打开应用并显示列表
- 新增一条完整病历
- 姓名为空时阻止保存
- 编辑已有病历
- 关闭未保存表单时出现提示
- 刷新或重启后数据仍存在

#### 删除

- 单条删除弹出二次确认
- 取消确认后数据仍存在
- 确认删除后数据消失

#### 查询和筛选

- 姓名模糊搜索
- 日期范围筛选
- 性别筛选
- 多条件组合筛选
- 重置筛选
- 分页切换
- 空结果状态

#### Excel 导入

- 正常文件导入
- 缺少必填表头
- 日期无法解析
- 姓名为空
- 年龄异常但保留原文
- 费用区间保留原文
- 逐行错误和警告展示
- 重复数据跳过
- 重复数据保留
- 重复数据覆盖
- 导入预览取消
- 导入失败时事务回滚
- 导入成功后保留批次快照

#### Excel 导出

- 导出当前筛选结果
- 导出全部记录
- 导出文件存在
- 文件表头顺序正确
- 行数与预期一致
- 中文字段和费用原文未损坏

#### 快捷键

- `Ctrl+N` 打开新增表单
- `Ctrl+F` 聚焦搜索框
- `Ctrl+S` 保存
- `Ctrl+Enter` 提交
- `Esc` 关闭弹窗
- `Ctrl+I` 打开导入
- `Ctrl+E` 触发导出

#### 分辨率和视觉

- 1024 x 768 下无内容遮挡
- 1366 x 768 下工具栏和表格正常
- 4K 缩放下弹窗不超出窗口
- 长姓名、长诊断、长治疗文本可以换行或截断展示
- 加载、错误、空数据状态布局稳定

## 16. Playwright 配置要求

已配置脚本：

```json
{
  "scripts": {
    "dev": "vite --config vite.config.ts",
    "dev:desktop": "electron-vite dev",
    "build": "electron-vite build",
    "package:portable": "npm run build && electron-builder --win portable",
    "test": "vitest run",
    "test:performance": "vitest run --config vitest.performance.config.ts",
    "test:e2e": "playwright test --project=web",
    "test:e2e:desktop": "npm run build && playwright test --project=desktop",
    "test:e2e:all": "npm run build && playwright test"
  }
}
```

Playwright 必须支持两种运行方式：

- 浏览器模式：验证 Vue 渲染层和交互。
- Electron 模式：验证便携应用启动、窗口、IPC 和文件选择流程。

测试失败时保存：

- 截图
- 视频或 trace
- 控制台日志
- 主进程日志
- 测试使用的临时数据库路径

## 17. 错误处理规范

错误提示必须说明：

- 发生了什么
- 哪一步失败
- 用户可以做什么

错误分类：

- `VALIDATION_ERROR`：字段校验失败
- `FILE_READ_ERROR`：文件无法读取
- `FILE_FORMAT_ERROR`：Excel 结构不正确
- `DATABASE_ERROR`：数据库操作失败
- `TRANSACTION_ERROR`：事务提交或回滚失败
- `EXPORT_ERROR`：导出失败
- `BACKUP_ERROR`：备份失败
- `CANCELLED`：用户取消操作

禁止将原始堆栈直接显示给用户。详细堆栈写入本地日志，用户界面显示可理解的提示。

## 18. 发布与便携版 exe

### 18.1 产物

目标产物：


- Windows x64 portable 单文件 exe
- 可选 zip 压缩包
- 版本号和构建时间写入应用关于页面

### 18.2 便携模式规则

- 不要求安装向导。
- electron-builder 固定使用 `https://npmmirror.com/mirrors/electron/` 获取 Electron 发行包，避免默认 GitHub 下载在当前网络环境超时。
- portable 使用 ZIP 而非默认 LZMA 解压格式，以适度增加文件体积换取低配置电脑更快的解压启动。
- 仅保留 `zh-CN`、`en-US` Electron 语言资源；前端依赖由 Vite 内联，ExcelJS 由主进程构建内联，生产包不重复复制 `node_modules`。
- 解压阶段显示原生启动提示；应用主窗口创建后立即显示统一背景色，避免长时间无反馈。
- 应用使用单实例锁，重复双击会聚焦已有窗口，不重复执行解压后的数据库进程。
- 首次启动自动创建数据目录。
- exe 所在目录不可写时，自动使用用户数据目录。
- 不覆盖用户数据库。
- 升级前自动备份。
- 应用退出时安全关闭 SQLite 连接。

### 18.3 发布前检查

- 在未安装 Node.js 的干净 Windows 电脑运行。
- 在无网络环境启动并完成基本 CRUD。
- 验证 WebView/Electron 运行库可用。
- 验证中文路径、中文文件名和中文 Excel。
- 验证 exe 复制到另一个目录后仍可运行。
- 验证旧版本数据迁移。
- 检查 Windows Defender 或 SmartScreen 提示，必要时进行代码签名。
- 正式签名构建使用 `npm run package:portable:signed`，并在构建环境提供 `CSC_LINK` 和 `CSC_KEY_PASSWORD`；缺少证书时强制签名构建必须失败，防止误把未签名产物当成正式版本。

## 19. 验收标准

### 功能验收

- 所有 9 个 Excel 字段可以导入和导出。
- 8075 条现有数据可以完成导入。
- 导入过程中错误行、警告行和重复行可区分。
- 重复策略按用户选择执行。
- 删除必须二次确认。
- 新增、编辑、删除和导入均可在操作记录中追踪。
- 新增、编辑、删除和导入保留完整操作快照。
- 导出的 Excel 可再次导入。

### 性能验收

- 低配置电脑下列表可持续操作。
- 8000 条数据导入时不出现长时间白屏或无响应。
- 常用查询 P95 不超过 300 毫秒。
- 页面在目标分辨率下没有重叠和溢出。

### 测试验收

- 单元测试、集成测试和 Playwright E2E 全部通过。
- 关键 E2E 场景覆盖率达到 100%。
- 视觉回归基线已生成并审核。
- 便携版 exe 在干净 Windows 环境启动成功。
- 导入、导出、备份、恢复和操作历史均有自动化测试。

## 20. 开发实施顺序

### 阶段一：基础骨架

- 初始化 Vue3、TypeScript、Electron、Element Plus
- 建立 preload 和类型化 IPC
- 建立 SQLite 和迁移机制
- 完成病历列表和分页
- 完成新增、编辑、查看、删除确认
- 建立基础 Playwright 测试框架

### 阶段二：Excel 能力

- Excel 文件读取
- 表头映射
- 逐行校验
- 重复检测和策略处理
- 导入预览
- 事务导入
- 当前筛选结果和全部数据导出

### 阶段三：可恢复和效率

- 操作日志
- 操作历史快照
- 自动备份和恢复
- 快捷键
- 性能优化和低分辨率适配

### 阶段四：发布验证

- 便携版 exe 构建
- 干净系统验证
- 视觉回归
- 大数据量性能测试
- 数据迁移测试
- 发布检查和版本归档

## 21. 已确认的第一版决策

1. 支持 Windows 10/11 64 位，输出 Windows x64 portable exe。
2. 定位为单机单用户，不做登录和权限体系。
3. 默认重复键为“日期 + 姓名 + 性别 + 年龄 + 诊断”，并可切换完整九字段、日期姓名、日期姓名性别年龄。
4. 年龄和费用始终保留 Excel 原始文本。
5. 数据库固定存放在 `%LOCALAPPDATA%`，不随 exe 移动，浏览器开发模式和 Electron 默认共用同一文件。
6. 第一版不包含打印；病历和操作记录通过 Excel 导出。
7. 第一版不包含备份加密；通过本机绑定、离线存储、显式导出和备份管理控制数据边界。

## 22. 完成定义

只有同时满足以下条件，项目才算完成：

- 功能需求全部实现。
- Excel 样例文件可完整导入并导出。
- 导入错误和重复行为符合本文档。
- 操作日志和导出可用，界面无撤销或重做入口。
- 低配置和目标分辨率测试通过。
- Playwright 关键场景全部通过。
- 便携版 exe 在干净 Windows 环境可以直接运行。
- 用户数据不会因升级、导入失败或误删除而无提示丢失。

## 23. 双模式开发与测试架构

为支持日常开发和无头浏览器自动化测试，系统采用双传输层、单业务层架构：

```text
Electron 渲染页面 -> preload / IPC ----┐
                                       ├-> Repository / Excel / Backup 服务 -> SQLite
浏览器页面 -> 本机 Vite HTTP API ------┘
```

约束：

- 两种模式使用相同的 Vue 页面、TypeScript 接口、Repository、Excel 服务和备份服务。
- 两种模式默认使用 `%LOCALAPPDATA%\ClinicRecords\data\clinic-records.db`。
- 浏览器不能直接使用 Electron IPC；浏览器模式通过仅监听 `127.0.0.1` 的开发 API 访问同一业务层。
- 浏览器开发 API 不进入 portable exe，仅用于本地开发和 Playwright 无头测试。
- 文件交互按运行时适配：Electron 使用系统文件对话框，浏览器使用上传和下载。
- 不同时运行两个模式执行写入、导入或恢复操作，避免恢复数据库时出现并发连接冲突。

运行命令：

```powershell
npm run dev              # 浏览器开发模式，http://127.0.0.1:5173
npm run dev:desktop      # Electron 开发模式
npm run test:e2e         # 无头 Chromium 业务回归
npm run test:e2e:desktop # Electron IPC 与共享 SQLite 冒烟验证
```

## 24. 最终验证记录

验证日期：2026-09-15。

| 验证项 | 命令或方式 | 结果 |
|---|---|---|
| ESLint | `npm run lint` | 通过，0 个错误 |
| TypeScript | `npm run typecheck` | 通过 |
| 单元与 SQLite 集成 | `npm test` | 2 个文件、8 个测试全部通过 |
| 浏览器 E2E | `npm run test:e2e` | 22/22 通过，无头 Chromium；覆盖连续新建、中文日期、行点击、明细遮罩、导入后页面高度、无批量入口、1024px 筛选间距、日期无排序及未保存三选项 |
| Electron E2E | `npm run test:e2e:desktop` | 4/4 通过，覆盖 IPC CRUD、主进程 bundle 的 Excel 导入导出、单实例保护与跨模式同库 |
| 独立视觉回归 | `npx playwright test tests/e2e/visual.spec.ts --project=web` | 3/3 通过，截图已人工检查 |
| 10 万条性能 | `npm run test:performance` | 1/1 通过，所有查询 P95 低于 300 ms |
| 生产依赖审计 | `npm audit --omit=dev --registry=https://registry.npmjs.org` | 0 个已知漏洞 |
| Electron 生产构建 | `npm run build` | 主进程、preload、renderer 均构建成功 |
| portable 构建 | `npm run package:portable` | 成功生成 ZIP 压缩的 Windows x64 单文件 exe |
| packaged 功能冒烟 | 启动 `dist\win-unpacked\诊所病历.exe` | SQLite、Excel 导入提交、Excel 导出回读均通过；第二实例退出且仅保留一个业务窗口 |
| portable 启动 | 启动最终单文件 exe 并分段计时 | 启动提示约 0.6-2.0 秒出现；热启动主窗口约 4.7-5.3 秒；第二次启动不产生第二个业务窗口 |

性能测试使用 100,000 条临时病历，当前机器测得：

| 查询 | P95 |
|---|---:|
| 首分页 | 1.69 ms |
| 精确关键词 | 41.25 ms |
| 短关键词 | 79.04 ms |
| 日期和性别组合筛选 | 33.79 ms |

真实 Excel 验证结果：

- `历史病历.xlsx` 共 8075 条病历，完成逐行预览和事务导入。
- 首次选择“保留重复”后数据库为 8075 条；再次导入选择“跳过重复”后仍为 8075 条。
- 导出文件保持“日期、姓名、性别、年龄、诊断、临床表现、治疗、备注、费用”九列表头顺序，`2岁10个月`、`100-80` 等原始文本未损坏。

分辨率验证结果：

- 1024 x 768、1366 x 768、1920 x 1080 均无页面级水平溢出。
- 三张截图位于 `test-results/artifacts/visual-*`，已检查工具栏、筛选、表格、固定操作列和分页区域，无控件重叠。

portable 启动优化验证结果：

| 指标 | 优化前 | 最终 ZIP 版 | 变化 |
|---|---:|---:|---:|
| 单文件大小 | 90,741,276 bytes | 123,615,040 bytes | 增加 36.2%，换取更快解压 |
| 解压目录 | 428,860,083 bytes / 75 文件 | 294,601,393 bytes / 21 文件 | 字节减少 31.3%，文件减少 72.0% |
| `app.asar` | 96,080,493 bytes，约 10,665 个条目 | 5,454,904 bytes，11 个条目 | 字节减少 94.3% |
| 热启动主窗口 | 约 6.6-6.7 秒 | 约 4.7-5.3 秒 | 缩短约 20%-30% |
| 首个可见反馈 | 启动前无反馈 | splash 约 0.6-2.0 秒出现 | 避免长时间空白等待 |

对照测试中，完全不压缩的 store 单文件为 295,582,948 bytes，热启动约 3.2-3.6 秒。考虑低配置电脑的磁盘复制、Defender 扫描和分发成本，最终采用 ZIP 作为速度与体积的折中。单文件 portable 每次启动仍必须解压 Electron 运行时，因此无法达到已解压目录版约 1-2 秒的热启动速度。

最终便携版产物：

```text
文件：dist\\clinic-records-0.1.0-x64.exe
大小：123,615,040 bytes
构建时间：2026-09-15 14:55:57
SHA-256：2D81988896CD3CABEA4F026AA070B7BC24DE88146176C456CBA6A84DEFBC66AC
```

### 24.1 发布边界与人工检查

- 当前 exe 未做数字签名，分发时 Windows SmartScreen 和 Defender 可能增加首次启动耗时并提示未知发布者。项目已提供 `npm run package:portable:signed` 强制签名入口；正式构建仍需配置有效的代码签名证书。
- 已在当前 Windows 主机验证 portable 启动；“未安装 Node.js 的全新 Windows 10/11 电脑”和实际目标低配置电脑仍属于部署现场的发布前人工验收，不应以开发机测试结果代替。
- 浏览器开发模式与 Electron 默认共享数据库，但不要让两个模式同时执行导入、恢复或其他写操作。
- 自动化测试使用 `CLINIC_RECORDS_DATA_DIR` 隔离数据，不会读写正式病历数据库。

## 25. 需求追踪矩阵

| 原始需求 | 已实现行为 | 主要验证证据 |
|---|---|---|
| 病历列表、增删改查 | SQLite 分页列表、组合筛选、新增、连续新建、查看、编辑和行尾单条删除；无复选框和批量删除 | Web E2E CRUD、连续录入、组合筛选及批量入口缺失断言；Repository 集成测试 |
| 删除二次确认 | 行尾单条删除进入确认框，取消不修改数据，确认前自动备份 | Web E2E 删除确认与取消 |
| 操作记录与导出 | 新增、编辑、删除、导入记录前后快照；支持详情懒加载和 Excel 导出；界面无撤销/重做入口 | 单元/集成历史快照；Web E2E 操作记录导出 |
| Excel 导入导出 | 九列结构、逐行有效/警告/错误/重复状态、跳过/保留/覆盖重复、事务提交 | 真实 8075 行导入、重复跳过、覆盖测试、缺表头测试、九列导出回读 |
| 浏览器与 exe 双模式 | 同一 Vue 页面；浏览器走本机 HTTP API，Electron 走 preload/IPC，共用服务和 SQLite | Electron E2E 跨模式同库测试 |
| 分辨率适配 | 固定视口弹性布局、表格内部滚动、操作列固定、弹窗限制视口尺寸 | 三档视觉测试；真实导入后页面高度断言 |
| 病历明细 | 单击行打开，720px 宽且点击遮罩不关闭 | Web E2E 行点击、宽度测量和遮罩点击 |
| 紧凑低分辨率布局 | 60px 顶栏、收窄纵向间距、1024px 筛选单行、640px 双列表单一屏展示 | Web E2E 几何尺寸与抽屉滚动高度断言；三档视觉截图 |
| 未保存保护 | 新建或编辑有变更时，取消和右上角关闭均询问保存；遮罩不关闭 | Web E2E 新建/编辑两条关闭路径及落库验证 |
| 日期本地化 | Element Plus 全局简体中文 locale | Web E2E 星期表头中文断言 |
| 键盘快捷键 | 新增、搜索、保存、导入、导出、刷新、Esc；不提供全选或批量删除快捷键，不拦截撤销/重做组合键 | Web E2E 常用快捷键和删除后 `Ctrl+Z` 不恢复 |
| 低配置流畅性 | SQLite 分页、FTS5 trigram、显式触发查询、操作详情按需加载、最多 100 行渲染；portable 使用 ZIP、依赖内联和语言裁剪并提供启动提示 | 10 万条四类查询 P95；8075 行真实 Excel E2E；三种压缩策略启动计时 |
| 数据安全 | 本地存储、自动/手动备份、20 份保留、完整性校验、恢复确认、版本化迁移 | Repository 备份恢复测试、Web E2E 备份恢复、迁移版本测试 |
| 便携版 exe | electron-builder portable 单文件，无安装向导；单实例保护；可选强制签名构建 | 最终 exe 构建、校验和、分段启动计时及 packaged 功能冒烟 |
